---
phase: 04-ai-tutor
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - TUTOR-01
  - TUTOR-02

must_haves:
  truths:
    - "User can navigate to Tutor tab and see the chat interface"
    - "User can submit a question and receive an answer drawn from uploaded materials"
    - "Response includes a Sources block with at least one clickable citation"
    - "Week filter restricts retrieval to that week's materials when selected"
  artifacts:
    - path: "frontend/components/TutorChat.tsx"
      provides: "Rendered chat UI with citations"
    - path: "backend/services/tutor_service.py"
      provides: "Working RAG pipeline"
  key_links:
    - from: "TutorChat.tsx"
      to: "POST /api/v1/tutor/chat"
      via: "apiFetch"
      pattern: "tutor/chat"
---

<objective>
UAT checkpoint: verify that Phase 4 success criteria are met by running the app and manually testing the AI tutor end-to-end.

Purpose: Confirms the RAG pipeline works against real embeddings and that citations display correctly before the phase is marked complete.
Output: Phase 4 UAT approved (or issues documented for gap closure).
</objective>

<execution_context>
@/Users/tanmaygoel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaygoel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ai-tutor/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT: Verify Phase 4 AI Tutor end-to-end</name>
  <what-built>
    Full AI tutor: backend RAG endpoint (tutor_service + router + IAM), frontend TutorChat component, and dashboard three-tab layout with collapsed syllabus section.
  </what-built>
  <how-to-verify>
    Start the stack: `cd /Users/tanmaygoel/CS/Sylli && sam build && sam local start-api --port 3001` (terminal 1), `cd frontend && npm run dev` (terminal 2).

    **Phase 4 success criteria (from ROADMAP.md):**

    1. **TUTOR-01 — Chat with materials:**
       - Log in, navigate to the dashboard
       - Confirm a three-tab bar is visible: Library | Tutor | Quiz (coming soon)
       - Click the Tutor tab
       - Type a question about one of your uploaded course materials (e.g., "What is covered in Week 2?")
       - Send the question
       - Verify: an AI answer appears in the chat that references content from your uploaded materials (not generic LLM knowledge)
       - Verify: animated typing indicator (three dots) showed while waiting

    2. **TUTOR-02 — Citations:**
       - Verify: the AI response ends with a "Sources:" block listing at least one citation
       - Citation format should be: `filename — Week N: Topic Label`
       - Click a citation link — verify it opens your source file in a new browser tab

    3. **Week filter:**
       - Select a specific week from the dropdown
       - Ask a question
       - Verify: the answer references only materials from that week (or returns the "no content found" message if no embedded materials exist for that week)

    4. **Syllabus collapse:**
       - Verify: the syllabus upload section shows a compact "Replace syllabus" button (not the full upload UI) when a syllabus is already loaded
       - Click "Replace syllabus" — verify the upload UI expands

    5. **No-content guard:**
       - Select a week that has no embedded materials
       - Ask any question
       - Verify: receive the message "I couldn't find relevant content in your materials..." rather than a generic LLM answer

    6. **Send button state:**
       - Send a question
       - Verify: the Send button is disabled while the response is loading

    **Note:** This test requires materials with `embed_status = "ready"` in DynamoDB. If no materials have been embedded yet, test the no-content guard path first, then confirm a material and wait for embedding before testing citation retrieval.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 criteria pass, or describe which criteria failed with details</resume-signal>
</task>

</tasks>

<verification>
UAT approved when:
- User can receive AI answers grounded in uploaded materials (TUTOR-01)
- Every answer includes a Sources block with clickable citations in the correct format (TUTOR-02)
- Week filter scopes retrieval correctly
- No-content guard returns a helpful message (not generic LLM output)
- Dashboard tab layout and syllabus collapse work correctly
</verification>

<success_criteria>
All Phase 4 ROADMAP success criteria confirmed by manual testing:
1. User can chat with AI tutor drawing from their uploaded course materials
2. Every response includes at least one citation naming source file and unit/week
Phase 4 marked complete in ROADMAP.md progress table.
</success_criteria>

<output>
After UAT approval, create `.planning/phases/04-ai-tutor/04-03-SUMMARY.md`
</output>
