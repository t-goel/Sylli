---
phase: 02-auth-and-syllabus
task: UAT (human verification)
total_tasks: 5 plans (all executed)
status: blocked
last_updated: 2026-03-14T23:36:43.293Z
---

<current_state>
Phase 2 (Auth and Syllabus) is fully built and code-complete. All 5 plans executed, 16/16 automated verification checks passed. Blocked on human UAT — requires Docker Desktop running to start `sam local start-api`.

Docker Desktop was not running during the auto-mode UAT gate (Plan 02-05), so it was auto-approved without live testing. Manual end-to-end verification is still needed before marking the phase complete.
</current_state>

<completed_work>

- Plan 02-01: Backend auth foundation — users DynamoDB table, PyJWT + passlib[bcrypt], auth_service, get_current_user FastAPI dependency. 28 tests passing (TDD).
- Plan 02-02: Auth API endpoints + syllabus enforcement — /register, /login endpoints, syllabus routes gated with Depends(get_current_user), user_id ownership check (404 on mismatch), CORS configured. 56 tests passing.
- Plan 02-03: Next.js scaffold + auth UI — App Router, AuthContext (localStorage), apiFetch wrapper, login/register page. Build verified clean.
- Plan 02-04: Dashboard + upload + timeline — auth guard layout, SyllabusUpload (raw fetch+FormData), WeekTimeline component. Zero TypeScript errors.
- Plan 02-05: UAT gate — auto-approved in auto-mode (Docker not running). Needs live testing.
- VERIFICATION.md created: 16/16 automated checks passed, status = human_needed.

</completed_work>

<remaining_work>

- Run the 5-scenario UAT with Docker Desktop running:
  1. Registration → redirect to /dashboard with username
  2. Syllabus upload → parsed timeline renders
  3. Logout and re-login → data access resumes
  4. Cross-user isolation → user B gets 404 on user A's syllabus
  5. Unauthenticated access → missing auth header returns 401
- Reply "approved" to mark phase complete and update ROADMAP.md

</remaining_work>

<decisions_made>

- bcrypt<4.0 pinned — passlib 1.7.4's detect_wrap_bug uses 200-byte string that bcrypt 5.x rejects
- HTTPBearer(auto_error=False) — returns 401 (not 403) for missing credentials
- SyllabusUpload uses raw fetch (not apiFetch) — apiFetch forces Content-Type: application/json, breaking multipart boundary
- JWT stored in localStorage — MVP simplicity per research recommendation
- CORS configured for localhost:3000 and *.amplifyapp.com

</decisions_made>

<blockers>

- Docker Desktop not running: `sam local start-api` fails with JSONDecodeError (SAM CLI ↔ Docker socket communication)
- Workaround found: `export DOCKER_HOST=unix:///Users/tanmaygoel/.docker/run/docker.sock` before running sam commands. BUT Docker Desktop must actually be open and running first.
- `sam build --use-container` also has a SAM CLI 1.154.0 cleanup bug (JSONDecodeError in container.delete). Use `sam build` (no --use-container) instead — native arm64 build works for local testing.

</blockers>

<context>
All code is done. This is purely a local dev environment issue — Docker Desktop just needs to be open. Once UAT passes, run `approved` response in Claude Code and the phase will be marked complete in ROADMAP.md. The --no-transition flag was set (spawned from plan-phase --auto), so after approval the system returns PHASE COMPLETE status.
</context>

<next_action>
1. Open Docker Desktop and wait for it to fully start (whale icon stops animating)
2. In terminal: `export DOCKER_HOST=unix:///Users/tanmaygoel/.docker/run/docker.sock && sam local start-api --port 3001`
3. In another terminal: `cd frontend && npm run dev`
4. Run the 5 UAT scenarios above
5. Reply "approved" to complete Phase 2
</next_action>
