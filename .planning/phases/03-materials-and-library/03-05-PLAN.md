---
phase: 03-materials-and-library
plan: "05"
type: execute
wave: 4
depends_on: ["03-04"]
files_modified: []
autonomous: false
requirements: [MAT-01, MAT-02, MAT-03, MAT-04, MAT-05, LIB-01, LIB-02]

must_haves:
  truths:
    - "User can upload a PDF or PPTX file and receive a response immediately without waiting for embedding"
    - "After upload, the AI-suggested week appears inline with Confirm and Change week buttons"
    - "User can change the suggested week via dropdown and confirm; a Processing badge appears"
    - "Processing badge transitions to a checkmark (or clears) when embedding completes"
    - "All uploaded materials appear in the library organized by week, including empty weeks"
    - "Unconfirmed materials show an Unconfirmed badge in the library"
    - "Clicking a material opens it in a new browser tab"
  artifacts:
    - path: "frontend/components/MaterialUpload.tsx"
      provides: "Material upload with inline confirmation"
    - path: "frontend/components/MaterialLibrary.tsx"
      provides: "Week-organized chronological library"
    - path: "backend/routers/materials.py"
      provides: "5 API endpoints for material management"
    - path: "backend/workers/embed_worker.py"
      provides: "Async embedding Lambda handler"
---

<objective>
UAT checkpoint: verify all Phase 3 success criteria with the application running locally.

Purpose: Human verification of the complete Phase 3 user-facing workflow before marking the phase complete. This checkpoint covers all 5 Phase 3 success criteria from ROADMAP.md.
Output: Phase 3 marked complete, STATE.md updated.
</objective>

<execution_context>
@/Users/tanmaygoel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaygoel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3 material upload and library pipeline:
- POST /api/v1/materials — uploads PDF/PPTX, returns AI week suggestion synchronously
- POST /api/v1/materials/{id}/confirm — triggers async embedding, returns immediately
- GET /api/v1/materials — lists all user materials (GSI query)
- GET /api/v1/materials/{id}/status — embed status polling endpoint
- GET /api/v1/materials/{id}/view — presigned S3 URL on-demand
- MaterialUpload component with inline week confirmation + embed status polling
- MaterialLibrary component with week-organized view, Unconfirmed badges, click-to-open
- EmbedWorkerFunction Lambda (async, extracts text, Titan Embed V2, writes to S3 Vectors)
  </what-built>
  <how-to-verify>
Start the backend (SAM local): `cd /Users/tanmaygoel/CS/Sylli && sam local start-api --port 3001`
Start the frontend: `cd /Users/tanmaygoel/CS/Sylli/frontend && npm run dev`
Visit: http://localhost:3000

Verify each Phase 3 success criterion:

1. **Non-blocking upload (MAT-01, MAT-04)**
   - Log in, navigate to the dashboard
   - Upload a PDF or PPTX file using the new "Upload Materials" section
   - Expected: response returns immediately (under ~10s for the AI suggestion); the file upload does NOT hang waiting for embedding

2. **AI week suggestion + inline confirmation (MAT-02, MAT-03)**
   - After upload, the row should expand showing: "'{filename}' → Week N: {topic}" with Confirm and Change week buttons
   - Click "Change week" — expected: a dropdown appears listing all weeks from your syllabus
   - Select a different week — expected: the displayed week updates
   - Click "Confirm" — expected: a "Processing..." badge appears immediately

3. **Embed status polling (MAT-04)**
   - After confirming, the "Processing..." badge should appear
   - Wait for the embed worker to complete (may take 30-120s depending on file size)
   - Expected: badge transitions to a checkmark or clears when done

4. **Library view — all weeks shown (LIB-01)**
   - The "Course Materials" section should show ALL weeks from the syllabus as section headers
   - Weeks with no materials should show "no materials yet"
   - The uploaded material should appear under its confirmed week

5. **Unconfirmed badge (MAT-03, LIB-01)**
   - Upload a second file, do NOT confirm it
   - Expected: it appears in the library under the AI-suggested week with an "Unconfirmed" badge

6. **Click to view original file (LIB-02)**
   - Click a confirmed material in the library
   - Expected: the file opens in a new browser tab (presigned S3 URL)
   - Expected: each click works even after 5+ minutes (URL is generated fresh per click)

Note: The embed worker Lambda runs async and requires AWS credentials + a deployed stack for full S3 Vectors testing. For local SAM testing, the embed worker invocation will fail silently (no EMBED_FUNCTION_NAME in local env) — the embed_status will remain 'pending'. This is acceptable for local UAT. The critical flows to verify locally are 1, 2, 4, 5, and 6.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 criteria pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
All Phase 3 ROADMAP.md success criteria:
1. Upload returns immediately without waiting for embedding — verified
2. AI suggests week, user can confirm or change — verified
3. Materials appear organized by week in library — verified
4. User can click material to view original file — verified
5. Embeddings stored with user_id and unit/week metadata (verified via CloudWatch logs on deployed stack)
</verification>

<success_criteria>
- All 6 UAT steps pass (or failures documented for gap closure)
- Phase 3 marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-materials-and-library/03-05-SUMMARY.md`
Update `.planning/STATE.md` — set Phase 3 as complete
Update `.planning/ROADMAP.md` — mark Phase 3 plans complete
</output>
