---
phase: 05-quiz-generator
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified: []
autonomous: false
requirements:
  - QUIZ-01
  - QUIZ-02
  - QUIZ-03

must_haves:
  truths:
    - "User can select a specific week and generate a quiz scoped to that week's materials"
    - "User can select 'All weeks' and generate a quiz spanning all course materials"
    - "After answering each question, an explanation with a source citation appears immediately"
    - "Results screen shows correct score and allows review of each question"
  artifacts:
    - path: "backend/services/quiz_service.py"
      provides: "generate_quiz() running end-to-end"
    - path: "frontend/components/QuizTab.tsx"
      provides: "Three-state quiz UI verified working"
  key_links:
    - from: "QuizTab scope screen"
      to: "POST /api/v1/quiz/generate"
      via: "apiFetch with week_number and count"
      pattern: "apiFetch.*quiz/generate"
---

<objective>
UAT checkpoint: verify all three Phase 5 success criteria are met with the full stack running.

Purpose: Confirms the quiz generator works end-to-end — scope selection, generation, step-through feedback with citations, and results screen.
Output: Human approval or documented issues to fix.
</objective>

<execution_context>
@/Users/tanmaygoel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaygoel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-quiz-generator/05-CONTEXT.md
@.planning/phases/05-quiz-generator/05-01-SUMMARY.md
@.planning/phases/05-quiz-generator/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT: Verify Phase 5 quiz generator success criteria</name>
  <action>Human verification of the complete quiz generator feature against all three ROADMAP success criteria. Start the full stack (SAM local on port 3001, Next.js dev on port 3000) and follow the verification steps below.</action>
  <what-built>
    Complete quiz generator:
    - Backend: POST /api/v1/quiz/generate (quiz_service + router + app.py registration)
    - Frontend: QuizTab three-state UI (scope screen, step-through quiz, results screen) wired into dashboard
  </what-built>
  <how-to-verify>
    Start the full stack (SAM local API on port 3001, Next.js dev on port 3000) then verify:

    Prerequisite: Log in and ensure at least one course has a syllabus uploaded and at least one material with embed_status=ready in at least one week.

    Success Criterion 1 — Week-scoped quiz (QUIZ-01):
    1. Click the "Quiz" tab in the dashboard (should now be enabled, not grayed-out)
    2. Select a specific week from the dropdown that has embedded materials
    3. Set question count to 5 (default)
    4. Click "Generate Quiz"
    5. Verify: spinner + "Generating your quiz..." appears while loading
    6. Verify: quiz screen shows "Question 1 of N" header
    7. Verify: four labeled options (A/B/C/D) appear
    8. Select an answer — verify it locks immediately (no confirm button)
    9. Verify: correct option turns green with a checkmark, wrong selection (if chosen) turns red with an X
    10. Verify: explanation appears below options with a clickable citation link in format "filename — Week N: Topic"
    11. Click the citation link — verify it opens the source file in a new tab (presigned S3 URL)

    Success Criterion 2 — All-weeks quiz (QUIZ-02):
    1. Complete or exit current quiz using "New quiz" button
    2. Select "All weeks" from dropdown
    3. Generate quiz
    4. Verify quiz generates successfully (questions drawn from across all materials)

    Success Criterion 3 — Results screen and review (QUIZ-03):
    1. Answer all questions in a quiz
    2. Click "Finish" on the last question
    3. Verify: results screen shows "You scored X out of N"
    4. Verify: each question is reviewable with the correct answer highlighted and explanation shown
    5. Click "New quiz" — verify scope screen reappears with all controls reset

    Edge cases to check:
    - Select a week with NO embedded materials — verify Generate button is disabled with inline message "No materials embedded for Week N"
    - Generation failure state: verify error message + Retry button appear
  </how-to-verify>
  <verify>Human confirms all success criteria pass by typing "approved"</verify>
  <done>All three ROADMAP success criteria for Phase 5 confirmed by human UAT</done>
  <resume-signal>Type "approved" if all success criteria pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 5 success criteria from ROADMAP.md:
1. A user can select a unit/week and generate a multiple-choice quiz scoped to only that unit's materials — QUIZ-01
2. A user can generate a quiz spanning all uploaded course materials — QUIZ-02
3. After answering each question, the user sees an explanation that cites the source material the answer was drawn from — QUIZ-03
</verification>

<success_criteria>
- All three ROADMAP success criteria confirmed by human UAT
- Quiz tab enabled and functional in dashboard
- Week-scoped and all-weeks quiz generation both work
- Immediate feedback with source citations verified
- Results screen and "New quiz" flow verified
</success_criteria>

<output>
After completion, create `.planning/phases/05-quiz-generator/05-03-SUMMARY.md`
</output>
