---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - template.yaml
  - SPEC.md
autonomous: true
requirements:
  - FOUND-03
must_haves:
  truths:
    - "No file in the project contains a HelloWorld identifier (logical ID, output key, directory name, test file)"
    - "The SAM template deploys the Lambda as SylliFunction with a Sylli event name"
    - "The hello_world/ directory is deleted — no dead code remains from the SAM scaffold"
    - "Legacy broken test files are deleted — no test files referencing HelloWorldFunction remain"
    - "SPEC.md infrastructure table references SylliFunction, not HelloWorldFunction"
  artifacts:
    - path: "template.yaml"
      provides: "SAM template with SylliFunction logical ID and updated Outputs"
      contains: "SylliFunction"
    - path: "SPEC.md"
      provides: "Documentation referencing SylliFunction"
      contains: "SylliFunction"
  key_links:
    - from: "template.yaml"
      to: "SylliFunction"
      via: "Resource logical ID rename"
      pattern: "SylliFunction:"
    - from: "template.yaml Outputs"
      to: "SylliFunction"
      via: "GetAtt SylliFunction.Arn and SylliFunction Role"
      pattern: "GetAtt SylliFunction"
---

<objective>
Purge all HelloWorld identifiers from the project. The SAM scaffold left `HelloWorldFunction` in six places in `template.yaml`, plus a dead `hello_world/` directory and two broken test files. This plan renames everything to Sylli and deletes the dead code.

Purpose: HelloWorld identifiers create confusion in CloudWatch logs, Lambda console, and CloudFormation. Deleting `hello_world/app.py` removes a dead entry point that could cause confusion about which handler is actually invoked.
Output: `template.yaml` with `SylliFunction` throughout, `SPEC.md` updated, `hello_world/` deleted, legacy test files deleted.
</objective>

<execution_context>
@/Users/tanmaygoel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaygoel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current state of template.yaml — exact locations of all HelloWorld identifiers -->

template.yaml HelloWorld locations (all must be renamed):
- Line 17:  Resource key `HelloWorldFunction:` → `SylliFunction:`
- Line 44:  SAM event name `HelloWorld:` → `Sylli:`
- Line 84:  Output key `HelloWorldApi:` → `SylliApi:`
- Line 88:  Output key `HelloWorldFunction:` → `SylliFunction:`
- Line 90:  `!GetAtt HelloWorldFunction.Arn` → `!GetAtt SylliFunction.Arn`
- Line 91:  Output key `HelloWorldFunctionIamRole:` → `SylliFunctionIamRole:`
- Line 93:  `!GetAtt HelloWorldFunctionRole.Arn` → `!GetAtt SylliFunctionRole.Arn`

Note: `samconfig.toml` `stack_name = "Sylli"` is already correct — do NOT touch it.
Note: `SyllabusTable` and `SyllabusBucket` logical IDs are already correct — do NOT rename them (renaming stateful resources in CloudFormation causes deletion + recreation = data loss).

Files to delete:
- `hello_world/` directory (contains app.py, __init__.py, requirements.txt)
- `tests/unit/test_handler.py`
- `tests/integration/test_api_gateway.py`

SPEC.md location to fix:
- Line 89: `HelloWorldFunction` → `SylliFunction` in the infrastructure table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all HelloWorld identifiers in template.yaml and update SPEC.md</name>
  <files>template.yaml, SPEC.md</files>
  <action>
Update `template.yaml` — make exactly these 7 changes and no others:

1. Line 17: Change resource logical ID from `HelloWorldFunction:` to `SylliFunction:`
2. Line 44: Change SAM event name from `HelloWorld:` to `Sylli:`
3. Line 84: Change Output key from `HelloWorldApi:` to `SylliApi:` (also update the Description from "Hello World" to "Sylli")
4. Line 88: Change Output key from `HelloWorldFunction:` to `SylliFunction:`
5. Line 90: Change `!GetAtt HelloWorldFunction.Arn` to `!GetAtt SylliFunction.Arn`
6. Line 91: Change Output key from `HelloWorldFunctionIamRole:` to `SylliFunctionIamRole:`
7. Line 93: Change `!GetAtt HelloWorldFunctionRole.Arn` to `!GetAtt SylliFunctionRole.Arn`

CRITICAL: Do NOT rename `SyllabusTable`, `SyllabusBucket`, or `ApplicationResourceGroup` — these are already correctly named or are stateful resources where a logical ID rename triggers CloudFormation deletion + replacement (data loss).

After updating `template.yaml`, update `SPEC.md`:
- Line 89: In the infrastructure table, change `HelloWorldFunction` to `SylliFunction` in the Lambda row.

Verify the template is still valid YAML after edits (no indentation errors introduced).
  </action>
  <verify>
    <automated>cd /Users/tanmaygoel/CS/Sylli && grep -c "HelloWorld" template.yaml && echo "FAIL: HelloWorld still present" || echo "PASS: no HelloWorld in template.yaml"</automated>
  </verify>
  <done>
    - `grep -c "HelloWorld" template.yaml` returns 0
    - `grep -c "SylliFunction" template.yaml` returns at least 5
    - `grep -c "HelloWorld" SPEC.md` returns 0
    - `template.yaml` is valid YAML (no parse errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete hello_world/ directory and legacy broken test files</name>
  <files>hello_world/app.py, hello_world/__init__.py, hello_world/requirements.txt, tests/unit/test_handler.py, tests/integration/test_api_gateway.py</files>
  <action>
Delete the following files and directory. Use the Bash tool to run these commands:

```bash
rm -rf /Users/tanmaygoel/CS/Sylli/hello_world/
rm -f /Users/tanmaygoel/CS/Sylli/tests/unit/test_handler.py
rm -f /Users/tanmaygoel/CS/Sylli/tests/integration/test_api_gateway.py
```

After deletion, verify:
- `hello_world/` directory no longer exists
- `tests/unit/test_handler.py` no longer exists
- `tests/integration/test_api_gateway.py` no longer exists

Also delete the `.aws-sam/` build cache directory if it exists — it may contain cached references to the old `HelloWorldFunction` logical ID that would confuse `sam build`:
```bash
rm -rf /Users/tanmaygoel/CS/Sylli/.aws-sam/
```

Do NOT delete `tests/unit/` or `tests/integration/` directories themselves — only the specific test files listed. Do NOT delete any files in `backend/`.
  </action>
  <verify>
    <automated>cd /Users/tanmaygoel/CS/Sylli && test ! -d hello_world && test ! -f tests/unit/test_handler.py && test ! -f tests/integration/test_api_gateway.py && echo "PASS: all deleted" || echo "FAIL: files still exist"</automated>
  </verify>
  <done>
    - `hello_world/` directory does not exist
    - `tests/unit/test_handler.py` does not exist
    - `tests/integration/test_api_gateway.py` does not exist
    - `grep -r "HelloWorld" . --include="*.py" --include="*.yaml" --include="*.toml" --include="*.md" | grep -v ".planning/"` returns no matches (or only comments/planning artifacts)
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "HelloWorld" template.yaml` — must return nothing
2. `grep -rn "HelloWorld" SPEC.md` — must return nothing
3. `ls hello_world/ 2>/dev/null && echo "FAIL" || echo "PASS: directory deleted"`
4. `ls tests/unit/test_handler.py 2>/dev/null && echo "FAIL" || echo "PASS: test deleted"`
5. `ls tests/integration/test_api_gateway.py 2>/dev/null && echo "FAIL" || echo "PASS: test deleted"`
6. `grep -c "SylliFunction" template.yaml` — must be >= 5
7. `python3 -c "import yaml; yaml.safe_load(open('template.yaml'))" && echo "PASS: valid YAML"` (run from project root)
</verification>

<success_criteria>
- Zero occurrences of `HelloWorld` in `template.yaml` and `SPEC.md`
- `template.yaml` has `SylliFunction` as the Lambda resource logical ID
- `template.yaml` Outputs section references `SylliFunction`, `SylliApi`, `SylliFunctionIamRole`
- `hello_world/` directory is gone
- `tests/unit/test_handler.py` and `tests/integration/test_api_gateway.py` are gone
- `template.yaml` is still valid YAML (no CloudFormation parse errors)
- `sam build` would use the new `SylliFunction` logical ID (verified by absence of `.aws-sam/` cache)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` using the summary template.
</output>
